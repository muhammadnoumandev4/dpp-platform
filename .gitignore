# dependencies
node_modules/

# build output
api/dist/
web/.next/
*.tsbuildinfo

# env (keep examples tracked; never commit real secrets)
.env
.env.*
!.env.example
!**/.env.example
!.env.local.example
!**/.env.local.example

# runtime data
api/uploads/
api/coverage/
web/test-results/
web/playwright-report/
api/test-results/

# editor/OS
.DS_Store

# local scratch / non-submission artifacts
tmp/
.claude/
*.pdf
CODEX_REVIEW_BRIEF.md

# scripts/ is part of the submission (integration + DB integrity smoke tests),
# but generated PDF output under it is not.
scripts/**/out/
